name: Create preview Builds For Android

on:
  push:
    branches: [main]

jobs:
  build_android:
    name: Build Android app
    type: build
    params:
      platform: android
      profile: preview
